# Backend API base URL (required)
# This is the origin of the backend server without trailing slash or /api suffix
VITE_API_BASE_URL=http://localhost:5032

# Optional: Full API URL override (includes /api suffix)
# If not provided, will be derived from VITE_API_BASE_URL
# VITE_API_URL=http://localhost:5032/api
